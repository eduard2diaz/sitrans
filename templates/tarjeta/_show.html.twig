<div class="modal-dialog modal-lg">
    <div class="modal-content">
        <div class="modal-header">
            <h5 class="modal-title">
                Detalles de la tarjeta
            </h5>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close"></button>
        </div>
        <div>
            <div class="modal-body" id="modal-body">
                <table class="table">
                    <tr>
                        <th>Código</th>
                        <td>{{ tarjeta.codigo }}</td>
                    </tr>
                    <tr>
                        <th>Tipo de tarjeta</th>
                        <td>{{ tarjeta.tipotarjeta }}</td>
                    </tr>
                    <tr>
                        <th>Tipo de combustible</th>
                        <td>{{ tarjeta.tipocombustible }}</td>
                    </tr>
                    <tr>
                        <th>Activo</th>
                        <td>{{ tarjeta.activo | colorear }}</td>
                    </tr>
                    <tr>
                        <th>Responsable</th>
                        <td>{{ tarjeta.responsable ? tarjeta.responsable : 'No tiene' }}</td>
                    </tr>
                   <tr>
                        <th>Cantidad de efectivo</th>
                        <td>{{ tarjeta.cantefectivo }}</td>
                    </tr>
                </table>
            </div>
            <div class="modal-footer">
                <a class="btn btn-metal btn-sm" data-dismiss="modal"><i class="flaticon-cancel"></i> Cancelar</a>
            </div>
        </div>
    </div>
</div>

