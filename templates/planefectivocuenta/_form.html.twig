{{form_start(form,{'attr':{
    'id': form_id|default('planefectivocuenta_new'),
    'class': 'm-form m-form--state'
}})}}

<div class="modal-body" id="modal-body">
    <div class="row">
        <div class="col-md-6">
            <div class="form-group m-form__group">
                <label>
                    {{form_label(form.cuenta)}}
                </label>
                <div>
                    {{form_widget(form.cuenta)}}
                    {{form_errors(form.cuenta)}}
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="form-group m-form__group">
                <label>
                    {{form_label(form.valor)}}
                </label>
                <div>
                    {{form_widget(form.valor)}}
                    {{form_errors(form.valor)}}
                </div>
            </div>
        </div>
    </div>
    <div class="row m--margin-top-10">
        <div class="col-md-6">
            <div class="form-group m-form__group">
                <label>
                    {{form_label(form.subelemento)}}
                </label>
                <div>
                    {{form_widget(form.subelemento)}}
                    {{form_errors(form.subelemento)}}
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="form-group m-form__group">
                <label>
                    {{form_label(form.centrocosto)}}
                </label>
                <div>
                    {{form_widget(form.centrocosto)}}
                    {{form_errors(form.centrocosto)}}
                </div>
            </div>
        </div>
    </div>
    {{form_errors(form)}}
    {{form_rest(form)}}
</div>
<div class="modal-footer">
    <a class="btn btn-metal btn-sm" data-dismiss="modal"><i class="flaticon-cancel"></i> Cancelar</a>
    <button class="btn btn-primary btn-sm mt-ladda-btn ladda-button" data-style="expand-right" type="submit">
        <i class="flaticon-edit-1 ladda-label"></i> {{ action |default('Guardar')}}
        <span class="ladda-spinner"></span>
    </button>
</div>
{{form_end(form)}}

