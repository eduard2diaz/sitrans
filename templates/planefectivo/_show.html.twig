{% extends 'layout/autenticado.html.twig' %}
 {% block page_title %}Plan de efectivo <small class="text-muted">{{ plan.getMesToString()~','~plan.anno }}</small>{% endblock page_title %}
{% block body %}
    <div class="m-portlet m-portlet--mobile">
        <div class="m-portlet__head">
            <div class="m-portlet__head-caption">
                <div class="m-portlet__head-title">
                    <span class="m-portlet__head-icon"><i class="flaticon-list-3"></i></span>
                    <h3 class="m-portlet__head-text">
                        Listado
                    </h3>
                </div>
            </div>

            <div class="m-portlet__head-tools">
                <ul class="m-portlet__nav">
                    <li class="m-portlet__nav-item">
                        <a data-href="{{ path('planefectivocuenta_new',{'id':plan.id}) }}" class="btn btn-sm edicion" data-toggle="m-tooltip" data-placement="top" title="" data-original-title="Registrar un plan de efectivo por cuenta">
                            <span><i class="flaticon-add"></i></span> Nuevo
                        </a>
                    </li>
                </ul>
            </div>

        </div>
        <div class="m-portlet__body">
            <!--begin: Datatable -->
            <div class="" id="local_data">
            </div>
            <table class="table table-hover m-table m-table--head-separator-brand" id="planefectivocuenta_table">
                <thead>
                <tr>
                    <th>
                        #
                    </th>
                    <th>
                        Cuenta
                    </th>
                    <th>
                        Centro de costo
                    </th>
                    <th>
                        Subelemento
                    </th>
                    <th>
                    </th>
                </tr>
                </thead>
                <tfoot>
                <tr>
                    <th>
                        #
                    </th>
                    <th>
                        Cuenta
                    </th>
                    <th>
                        Centro de costo
                    </th>
                    <th>
                        Subelemento
                    </th>
                    <th>
                    </th>
                </tr>
                </tfoot>
            </table>
            <!--end: Datatable -->
        </div>
    </div>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('script/entity/planefectivocuenta.js','sitrans') }}"></script>
    <script>
        var planefectivo='{{ plan.id }}';
        planefectivocuenta.init();
    </script>
{% endblock javascripts %}





