<!DOCTYPE html>

<title>Edit Somaton</title>

{% block body %}
    <h1>Edit Somaton</h1>

    {{ include('somaton/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('somaton_index') }}">back to list</a>

    {{ include('somaton/_delete_form.html.twig') }}
{% endblock %}