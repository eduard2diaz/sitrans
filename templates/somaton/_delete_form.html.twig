<form method="post" action="{{ path('somaton_delete', {'id': somaton.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ somaton.id) }}">
    <button class="btn">Delete</button>
</form>