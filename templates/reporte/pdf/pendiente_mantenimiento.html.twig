<!DOCTYPE html>
<html>
<meta charset="UTF-8"/>
<head>
    <title>Pendientes a mantenimiento</title>
    <link href="{{ absolute_url(asset('vendors/base/vendors.bundle.css','template')) }}" rel="stylesheet"
          type="text/css"/>
    <link href="{{ absolute_url(asset('demo/demo9/base/style.bundle.css','template')) }}" rel="stylesheet"
          type="text/css"/>

</head>
<body>
<h1 class="text-center m--margin-bottom-40">
    Pendientes a mantenimiento
</h1>
<table class="table">
    <thead>
    <tr>
        <th>Matrícula</th>
        <th>Kilómetros recorridos</th>
        <th>Diferencia</th>
    </tr>
    </thead>
    {% for obj in data %}
        <tr>
            <td>{{ obj.matricula }}</td>
            <td>{{ obj.kmsxrecorridos }}</td>
            <td>{{ obj.diferencia }}</td>
        </tr>
    {% endfor %}
    <tfoot>
    <tr>
        <th>Matrícula</th>
        <th>Kilómetros recorridos</th>
        <th>Diferencia</th>
    </tr>
    </tfoot>
</table>
</body>
</html>