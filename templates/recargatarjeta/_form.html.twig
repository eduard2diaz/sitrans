{{form_start(form,{'attr':{
    'id': form_id|default('recargatarjeta_new'),
    'class': 'm-form m-form--state'
}})}}

<div class="modal-body" id="modal-body">
    <div class="row">
        <div class="col-md-6">
            <div class="form-group m-form__group">
                <label>
                    {{form_label(form.tarjeta)}}
                </label>
                <div>
                    {{form_widget(form.tarjeta)}}
                    {{form_errors(form.tarjeta)}}
                </div>
            </div>
            {{form_errors(form)}}
        </div>
        <div class="col-md-6">
            <div class="form-group m-form__group">
                <label>
                    {{form_label(form.fecha)}}
                </label>
                <div>
                    {{form_widget(form.fecha)}}
                    {{form_errors(form.fecha)}}
                </div>
            </div>
        </div>
    </div>
    <div class="clearfix"></div>
    <div class="row margin-top-10">
        <div class="col-md-6">
            <div class="form-group m-form__group">
                <label>
                    {{form_label(form.cantidadlitros)}}
                </label>
                <div>
                    {{form_widget(form.cantidadlitros)}}
                    {{form_errors(form.cantidadlitros)}}
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="form-group m-form__group">
                <label>
                    {{form_label(form.cantidadefectivo)}}
                </label>
                <div>
                    {{form_widget(form.cantidadefectivo)}}
                    {{form_errors(form.cantidadefectivo)}}
                </div>
            </div>
        </div>
    </div>
</div>
<div class="modal-footer">
    <a class="btn btn-metal" data-dismiss="modal"><i class="flaticon-cancel"></i> Cancelar</a>
    <button class="btn btn-primary mt-ladda-btn ladda-button" data-style="expand-right" type="submit">
        <i class="flaticon-edit-1 ladda-label"></i> {{ action |default('Guardar')}}
        <span class="ladda-spinner"></span>
    </button>
</div>
{{form_end(form)}}

